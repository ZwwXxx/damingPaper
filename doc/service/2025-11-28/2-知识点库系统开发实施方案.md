# 知识点库系统 - 开发实施方案

> **文档版本**: v1.0  
> **创建日期**: 2025-11-28  
> **适用人员**: 后端开发、前端开发、测试人员

---

## 📋 目录

- [一、开发准备](#一开发准备)
- [二、后端开发指南](#二后端开发指南)
- [三、前端开发指南](#三前端开发指南)
- [四、开发规范](#四开发规范)
- [五、测试方案](#五测试方案)

---

## 一、开发准备

### 1.1 环境准备

**后端环境**:
```bash
# JDK 1.8+
java -version

# Maven 3.6+
mvn -version

# MySQL 5.7+
mysql --version

# Redis（可选，用于缓存）
redis-cli --version
```

**前端环境**:
```bash
# Node.js 12+
node -version

# npm或yarn
npm -version
```

### 1.2 数据库初始化

```bash
# 1. 连接到MySQL数据库
mysql -u root -p

# 2. 执行SQL脚本
source /path/to/project/daming-admin/sql/2025-11-28/1-知识点库系统表结构.sql
```

### 1.3 项目结构规划

```
daming-admin
├── ruoyi-admin
│   └── src/main/java/com/ruoyi/web/controller
│       └── knowledge                    # 知识点模块控制器
│           ├── admin/                   # 后台管理
│           │   ├── KnowledgeSubjectController.java
│           │   ├── KnowledgeChapterController.java
│           │   ├── KnowledgePointController.java
│           │   └── KnowledgeTagController.java
│           └── student/                 # 前台学生端
│               ├── KnowledgeStudentController.java
│               ├── KnowledgeLearnController.java
│               └── KnowledgeInteractionController.java
├── ruoyi-system
│   └── src/main/java/com/ruoyi
│       ├── domain/                      # 实体类
│       │   ├── KnowledgeSubject.java
│       │   ├── KnowledgeChapter.java
│       │   ├── KnowledgePoint.java
│       │   ├── KnowledgeTag.java
│       │   ├── KnowledgeCollect.java
│       │   ├── KnowledgeLike.java
│       │   ├── KnowledgeComment.java
│       │   └── KnowledgeLearnRecord.java
│       ├── mapper/                      # Mapper接口
│       │   ├── KnowledgeSubjectMapper.java
│       │   ├── KnowledgeChapterMapper.java
│       │   ├── KnowledgePointMapper.java
│       │   └── ...
│       └── service/                     # Service服务
│           ├── IKnowledgeSubjectService.java
│           ├── IKnowledgeChapterService.java
│           ├── IKnowledgePointService.java
│           └── impl/
│               ├── KnowledgeSubjectServiceImpl.java
│               ├── KnowledgeChapterServiceImpl.java
│               └── KnowledgePointServiceImpl.java
└── ruoyi-system/resources/mapper
    └── knowledge/                       # MyBatis XML映射文件
        ├── KnowledgeSubjectMapper.xml
        ├── KnowledgeChapterMapper.xml
        ├── KnowledgePointMapper.xml
        └── ...

daming-front
└── src
    ├── api
    │   └── knowledge.js                 # 知识点API接口
    ├── views
    │   └── knowledge/                   # 知识点页面
    │       ├── index.vue                # 知识点导航页
    │       ├── list.vue                 # 知识点列表页
    │       ├── detail.vue               # 知识点详情页
    │       ├── create.vue               # 创建知识点
    │       └── learn/                   # 学习中心
    │           ├── index.vue
    │           ├── myCollect.vue
    │           └── myContribute.vue
    └── components
        └── knowledge/                   # 知识点组件
            ├── ChapterTree.vue          # 章节树组件
            ├── PointCard.vue            # 知识点卡片
            ├── MarkdownEditor.vue       # Markdown编辑器
            └── MarkdownViewer.vue       # Markdown查看器
```

---

## 二、后端开发指南

### 2.1 实体类开发

#### 示例：KnowledgePoint.java

```java
package com.ruoyi.knowledge.domain;

import com.fasterxml.jackson.annotation.JsonFormat;
import com.ruoyi.common.annotation.Excel;
import com.ruoyi.common.core.domain.BaseEntity;
import org.apache.commons.lang3.builder.ToStringBuilder;
import org.apache.commons.lang3.builder.ToStringStyle;

import java.util.Date;
import java.util.List;

/**
 * 知识点对象 knowledge_point
 * 
 * @author ruoyi
 * @date 2025-11-28
 */
public class KnowledgePoint extends BaseEntity {
    private static final long serialVersionUID = 1L;

    /** 知识点ID */
    private Long pointId;

    /** 所属科目ID */
    @Excel(name = "所属科目ID")
    private Long subjectId;

    /** 所属章节ID */
    @Excel(name = "所属章节ID")
    private Long chapterId;

    /** 知识点标题 */
    @Excel(name = "知识点标题")
    private String title;

    /** 摘要 */
    @Excel(name = "摘要")
    private String summary;

    /** 知识点内容（Markdown格式） */
    private String content;

    /** HTML内容（前端渲染用） */
    private String contentHtml;

    /** 难度等级（1-简单 2-中等 3-困难） */
    @Excel(name = "难度等级", readConverterExp = "1=简单,2=中等,3=困难")
    private Integer difficulty;

    /** 重要程度（1-了解 2-理解 3-掌握 4-精通） */
    @Excel(name = "重要程度", readConverterExp = "1=了解,2=理解,3=掌握,4=精通")
    private Integer importance;

    /** 作者ID */
    private Long authorId;

    /** 作者名称 */
    @Excel(name = "作者名称")
    private String authorName;

    /** 浏览次数 */
    private Integer viewCount;

    /** 点赞数 */
    private Integer likeCount;

    /** 收藏数 */
    private Integer collectCount;

    /** 评论数 */
    private Integer commentCount;

    /** 学习人数 */
    private Integer learnCount;

    /** 是否推荐（0-否 1-是） */
    private Integer isRecommend;

    /** 是否置顶（0-否 1-是） */
    private Integer isTop;

    /** 状态（0-草稿 1-已发布 2-审核中 3-已下架） */
    @Excel(name = "状态", readConverterExp = "0=草稿,1=已发布,2=审核中,3=已下架")
    private Integer status;

    /** 审核状态（0-待审核 1-通过 2-拒绝） */
    @Excel(name = "审核状态", readConverterExp = "0=待审核,1=通过,2=拒绝")
    private Integer auditStatus;

    /** 审核备注 */
    private String auditRemark;

    /** 版本号 */
    private Integer version;

    /** 发布时间 */
    @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    private Date publishTime;

    // 扩展字段（不在数据库中）
    
    /** 科目名称 */
    private String subjectName;

    /** 章节名称 */
    private String chapterName;

    /** 标签列表 */
    private List<KnowledgeTag> tags;

    /** 当前用户是否已点赞 */
    private Boolean isLiked;

    /** 当前用户是否已收藏 */
    private Boolean isCollected;

    // Getter和Setter方法...
    
    public Long getPointId() {
        return pointId;
    }

    public void setPointId(Long pointId) {
        this.pointId = pointId;
    }

    public Long getSubjectId() {
        return subjectId;
    }

    public void setSubjectId(Long subjectId) {
        this.subjectId = subjectId;
    }

    // ... 其他getter/setter ...

    @Override
    public String toString() {
        return new ToStringBuilder(this, ToStringStyle.MULTI_LINE_STYLE)
                .append("pointId", getPointId())
                .append("subjectId", getSubjectId())
                .append("chapterId", getChapterId())
                .append("title", getTitle())
                .append("summary", getSummary())
                .append("difficulty", getDifficulty())
                .append("importance", getImportance())
                .append("authorId", getAuthorId())
                .append("authorName", getAuthorName())
                .append("viewCount", getViewCount())
                .append("likeCount", getLikeCount())
                .append("collectCount", getCollectCount())
                .append("status", getStatus())
                .append("createTime", getCreateTime())
                .append("updateTime", getUpdateTime())
                .toString();
    }
}
```

### 2.2 Mapper接口开发

#### 示例：KnowledgePointMapper.java

```java
package com.ruoyi.knowledge.mapper;

import com.ruoyi.knowledge.domain.KnowledgePoint;
import org.apache.ibatis.annotations.Param;

import java.util.List;

/**
 * 知识点Mapper接口
 * 
 * @author ruoyi
 * @date 2025-11-28
 */
public interface KnowledgePointMapper {
    /**
     * 查询知识点
     * 
     * @param pointId 知识点主键
     * @return 知识点
     */
    public KnowledgePoint selectKnowledgePointByPointId(Long pointId);

    /**
     * 查询知识点列表
     * 
     * @param knowledgePoint 知识点
     * @return 知识点集合
     */
    public List<KnowledgePoint> selectKnowledgePointList(KnowledgePoint knowledgePoint);

    /**
     * 查询热门知识点
     * 
     * @param limit 数量限制
     * @return 知识点集合
     */
    public List<KnowledgePoint> selectHotKnowledgePoints(@Param("limit") Integer limit);

    /**
     * 查询最新知识点
     * 
     * @param limit 数量限制
     * @return 知识点集合
     */
    public List<KnowledgePoint> selectLatestKnowledgePoints(@Param("limit") Integer limit);

    /**
     * 查询推荐知识点
     * 
     * @param limit 数量限制
     * @return 知识点集合
     */
    public List<KnowledgePoint> selectRecommendKnowledgePoints(@Param("limit") Integer limit);

    /**
     * 新增知识点
     * 
     * @param knowledgePoint 知识点
     * @return 结果
     */
    public int insertKnowledgePoint(KnowledgePoint knowledgePoint);

    /**
     * 修改知识点
     * 
     * @param knowledgePoint 知识点
     * @return 结果
     */
    public int updateKnowledgePoint(KnowledgePoint knowledgePoint);

    /**
     * 删除知识点
     * 
     * @param pointId 知识点主键
     * @return 结果
     */
    public int deleteKnowledgePointByPointId(Long pointId);

    /**
     * 批量删除知识点
     * 
     * @param pointIds 需要删除的数据主键集合
     * @return 结果
     */
    public int deleteKnowledgePointByPointIds(Long[] pointIds);

    /**
     * 增加浏览次数
     * 
     * @param pointId 知识点ID
     * @return 结果
     */
    public int increaseViewCount(Long pointId);

    /**
     * 增加点赞数
     * 
     * @param pointId 知识点ID
     * @return 结果
     */
    public int increaseLikeCount(Long pointId);

    /**
     * 减少点赞数
     * 
     * @param pointId 知识点ID
     * @return 结果
     */
    public int decreaseLikeCount(Long pointId);

    /**
     * 增加收藏数
     * 
     * @param pointId 知识点ID
     * @return 结果
     */
    public int increaseCollectCount(Long pointId);

    /**
     * 减少收藏数
     * 
     * @param pointId 知识点ID
     * @return 结果
     */
    public int decreaseCollectCount(Long pointId);

    /**
     * 增加评论数
     * 
     * @param pointId 知识点ID
     * @return 结果
     */
    public int increaseCommentCount(Long pointId);

    /**
     * 减少评论数
     * 
     * @param pointId 知识点ID
     * @return 结果
     */
    public int decreaseCommentCount(Long pointId);
}
```

### 2.3 Service接口开发

#### 示例：IKnowledgePointService.java

```java
package com.ruoyi.knowledge.service;

import com.ruoyi.knowledge.domain.KnowledgePoint;
import com.ruoyi.knowledge.domain.vo.KnowledgePointVO;

import java.util.List;

/**
 * 知识点Service接口
 * 
 * @author ruoyi
 * @date 2025-11-28
 */
public interface IKnowledgePointService {
    /**
     * 查询知识点
     * 
     * @param pointId 知识点主键
     * @return 知识点
     */
    public KnowledgePointVO selectKnowledgePointByPointId(Long pointId, Long userId);

    /**
     * 查询知识点列表
     * 
     * @param knowledgePoint 知识点
     * @return 知识点集合
     */
    public List<KnowledgePointVO> selectKnowledgePointList(KnowledgePoint knowledgePoint, Long userId);

    /**
     * 查询热门知识点
     * 
     * @param limit 数量限制
     * @return 知识点集合
     */
    public List<KnowledgePointVO> selectHotKnowledgePoints(Integer limit, Long userId);

    /**
     * 查询最新知识点
     * 
     * @param limit 数量限制
     * @return 知识点集合
     */
    public List<KnowledgePointVO> selectLatestKnowledgePoints(Integer limit, Long userId);

    /**
     * 查询推荐知识点
     * 
     * @param limit 数量限制
     * @return 知识点集合
     */
    public List<KnowledgePointVO> selectRecommendKnowledgePoints(Integer limit, Long userId);

    /**
     * 新增知识点
     * 
     * @param knowledgePoint 知识点
     * @return 结果
     */
    public int insertKnowledgePoint(KnowledgePoint knowledgePoint);

    /**
     * 修改知识点
     * 
     * @param knowledgePoint 知识点
     * @return 结果
     */
    public int updateKnowledgePoint(KnowledgePoint knowledgePoint);

    /**
     * 批量删除知识点
     * 
     * @param pointIds 需要删除的知识点主键集合
     * @return 结果
     */
    public int deleteKnowledgePointByPointIds(Long[] pointIds);

    /**
     * 删除知识点信息
     * 
     * @param pointId 知识点主键
     * @return 结果
     */
    public int deleteKnowledgePointByPointId(Long pointId);

    /**
     * 审核知识点
     * 
     * @param pointId 知识点ID
     * @param auditStatus 审核状态
     * @param auditRemark 审核备注
     * @return 结果
     */
    public int auditKnowledgePoint(Long pointId, Integer auditStatus, String auditRemark);

    /**
     * 增加浏览次数
     * 
     * @param pointId 知识点ID
     * @return 结果
     */
    public int increaseViewCount(Long pointId);
}
```

### 2.4 Controller控制器开发

#### 示例：学生端控制器

```java
package com.ruoyi.web.controller.knowledge.student;

import com.ruoyi.common.annotation.Log;
import com.ruoyi.common.core.controller.BaseController;
import com.ruoyi.common.core.domain.AjaxResult;
import com.ruoyi.common.core.page.TableDataInfo;
import com.ruoyi.common.enums.BusinessType;
import com.ruoyi.common.utils.SecurityUtils;
import com.ruoyi.knowledge.domain.KnowledgePoint;
import com.ruoyi.knowledge.domain.vo.KnowledgePointVO;
import com.ruoyi.knowledge.service.IKnowledgePointService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.security.access.prepost.PreAuthorize;
import org.springframework.web.bind.annotation.*;

import java.util.List;

/**
 * 知识点学生端Controller
 * 
 * @author ruoyi
 * @date 2025-11-28
 */
@RestController
@RequestMapping("/student/knowledge")
public class KnowledgeStudentController extends BaseController {
    
    @Autowired
    private IKnowledgePointService knowledgePointService;

    /**
     * 查询知识点列表
     */
    @GetMapping("/points")
    public TableDataInfo list(KnowledgePoint knowledgePoint) {
        startPage();
        Long userId = SecurityUtils.getUserId();
        List<KnowledgePointVO> list = knowledgePointService.selectKnowledgePointList(knowledgePoint, userId);
        return getDataTable(list);
    }

    /**
     * 获取知识点详细信息
     */
    @GetMapping("/point/{pointId}")
    public AjaxResult getInfo(@PathVariable("pointId") Long pointId) {
        Long userId = SecurityUtils.getUserId();
        // 增加浏览次数
        knowledgePointService.increaseViewCount(pointId);
        return success(knowledgePointService.selectKnowledgePointByPointId(pointId, userId));
    }

    /**
     * 获取热门知识点
     */
    @GetMapping("/point/hot")
    public AjaxResult getHotPoints(@RequestParam(defaultValue = "10") Integer limit) {
        Long userId = SecurityUtils.getUserId();
        return success(knowledgePointService.selectHotKnowledgePoints(limit, userId));
    }

    /**
     * 获取最新知识点
     */
    @GetMapping("/point/latest")
    public AjaxResult getLatestPoints(@RequestParam(defaultValue = "10") Integer limit) {
        Long userId = SecurityUtils.getUserId();
        return success(knowledgePointService.selectLatestKnowledgePoints(limit, userId));
    }

    /**
     * 获取推荐知识点
     */
    @GetMapping("/point/recommend")
    public AjaxResult getRecommendPoints(@RequestParam(defaultValue = "10") Integer limit) {
        Long userId = SecurityUtils.getUserId();
        return success(knowledgePointService.selectRecommendKnowledgePoints(limit, userId));
    }

    /**
     * 新增知识点（学生创建，需审核）
     */
    @Log(title = "知识点", businessType = BusinessType.INSERT)
    @PostMapping("/point/create")
    public AjaxResult add(@RequestBody KnowledgePoint knowledgePoint) {
        // 设置作者信息
        knowledgePoint.setAuthorId(SecurityUtils.getUserId());
        knowledgePoint.setAuthorName(SecurityUtils.getUsername());
        // 设置状态为审核中
        knowledgePoint.setStatus(2);
        knowledgePoint.setAuditStatus(0);
        return toAjax(knowledgePointService.insertKnowledgePoint(knowledgePoint));
    }

    /**
     * 修改知识点（仅限作者）
     */
    @Log(title = "知识点", businessType = BusinessType.UPDATE)
    @PutMapping("/point/update")
    public AjaxResult edit(@RequestBody KnowledgePoint knowledgePoint) {
        // 验证是否为作者本人
        Long userId = SecurityUtils.getUserId();
        KnowledgePointVO point = knowledgePointService.selectKnowledgePointByPointId(knowledgePoint.getPointId(), userId);
        if (!point.getAuthorId().equals(userId)) {
            return error("无权修改他人的知识点");
        }
        // 修改后需要重新审核
        knowledgePoint.setStatus(2);
        knowledgePoint.setAuditStatus(0);
        return toAjax(knowledgePointService.updateKnowledgePoint(knowledgePoint));
    }
}
```

### 2.5 Markdown渲染工具类

```java
package com.ruoyi.common.utils;

import org.commonmark.node.Node;
import org.commonmark.parser.Parser;
import org.commonmark.renderer.html.HtmlRenderer;

/**
 * Markdown工具类
 * 
 * @author ruoyi
 */
public class MarkdownUtils {
    
    private static final Parser parser = Parser.builder().build();
    private static final HtmlRenderer renderer = HtmlRenderer.builder().build();

    /**
     * Markdown转HTML
     * 
     * @param markdown Markdown文本
     * @return HTML文本
     */
    public static String markdownToHtml(String markdown) {
        if (markdown == null || markdown.isEmpty()) {
            return "";
        }
        Node document = parser.parse(markdown);
        return renderer.render(document);
    }

    /**
     * 提取摘要（取前200个字符）
     * 
     * @param markdown Markdown文本
     * @return 摘要
     */
    public static String extractSummary(String markdown) {
        if (markdown == null || markdown.isEmpty()) {
            return "";
        }
        // 去除Markdown标记
        String plain = markdown.replaceAll("#+ ", "")
                               .replaceAll("\\*\\*", "")
                               .replaceAll("\\[([^\\]]+)\\]\\([^\\)]+\\)", "$1")
                               .replaceAll("```[\\s\\S]*?```", "");
        
        // 截取前200个字符
        if (plain.length() > 200) {
            return plain.substring(0, 200) + "...";
        }
        return plain;
    }
}
```

---

## 三、前端开发指南

### 3.1 API接口封装

#### 文件：src/api/knowledge.js

```javascript
import request from '@/utils/request'

// ==================== 科目相关 ====================

// 获取科目列表
export function getSubjectList(query) {
  return request({
    url: '/student/knowledge/subjects',
    method: 'get',
    params: query
  })
}

// ==================== 章节相关 ====================

// 获取章节树
export function getChapterTree(subjectId) {
  return request({
    url: `/student/knowledge/chapters/${subjectId}`,
    method: 'get'
  })
}

// ==================== 知识点相关 ====================

// 获取知识点列表
export function getPointList(query) {
  return request({
    url: '/student/knowledge/points',
    method: 'get',
    params: query
  })
}

// 获取知识点详情
export function getPointDetail(pointId) {
  return request({
    url: `/student/knowledge/point/${pointId}`,
    method: 'get'
  })
}

// 获取热门知识点
export function getHotPoints(limit = 10) {
  return request({
    url: '/student/knowledge/point/hot',
    method: 'get',
    params: { limit }
  })
}

// 获取最新知识点
export function getLatestPoints(limit = 10) {
  return request({
    url: '/student/knowledge/point/latest',
    method: 'get',
    params: { limit }
  })
}

// 获取推荐知识点
export function getRecommendPoints(limit = 10) {
  return request({
    url: '/student/knowledge/point/recommend',
    method: 'get',
    params: { limit }
  })
}

// 创建知识点
export function createPoint(data) {
  return request({
    url: '/student/knowledge/point/create',
    method: 'post',
    data: data
  })
}

// 更新知识点
export function updatePoint(data) {
  return request({
    url: '/student/knowledge/point/update',
    method: 'put',
    data: data
  })
}

// ==================== 互动相关 ====================

// 点赞/取消点赞
export function toggleLike(pointId) {
  return request({
    url: `/student/knowledge/like/${pointId}`,
    method: 'post'
  })
}

// 收藏/取消收藏
export function toggleCollect(pointId) {
  return request({
    url: `/student/knowledge/collect/${pointId}`,
    method: 'post'
  })
}

// 获取评论列表
export function getCommentList(pointId, params) {
  return request({
    url: `/student/knowledge/comments/${pointId}`,
    method: 'get',
    params: params
  })
}

// 发表评论
export function addComment(data) {
  return request({
    url: '/student/knowledge/comment',
    method: 'post',
    data: data
  })
}

// ==================== 学习记录 ====================

// 记录学习行为
export function recordLearn(data) {
  return request({
    url: '/student/knowledge/learn/record',
    method: 'post',
    data: data
  })
}

// 获取我的学习记录
export function getMyLearnRecords(params) {
  return request({
    url: '/student/knowledge/learn/my',
    method: 'get',
    params: params
  })
}

// 获取学习统计
export function getLearnStats() {
  return request({
    url: '/student/knowledge/learn/stats',
    method: 'get'
  })
}
```

### 3.2 知识点列表页面

#### 文件：src/views/knowledge/list.vue

```vue
<template>
  <div class="knowledge-list-container">
    <!-- 筛选栏 -->
    <el-card class="filter-card" shadow="never">
      <el-form :inline="true" :model="queryParams" class="filter-form">
        <el-form-item label="科目">
          <el-select v-model="queryParams.subjectId" placeholder="请选择科目" clearable @change="handleSubjectChange">
            <el-option
              v-for="subject in subjectList"
              :key="subject.subjectId"
              :label="subject.subjectName"
              :value="subject.subjectId"
            />
          </el-select>
        </el-form-item>
        
        <el-form-item label="章节">
          <el-select v-model="queryParams.chapterId" placeholder="请选择章节" clearable>
            <el-option
              v-for="chapter in chapterList"
              :key="chapter.chapterId"
              :label="chapter.chapterName"
              :value="chapter.chapterId"
            />
          </el-select>
        </el-form-item>
        
        <el-form-item label="难度">
          <el-select v-model="queryParams.difficulty" placeholder="请选择难度" clearable>
            <el-option label="简单" :value="1" />
            <el-option label="中等" :value="2" />
            <el-option label="困难" :value="3" />
          </el-select>
        </el-form-item>
        
        <el-form-item label="关键词">
          <el-input v-model="queryParams.keyword" placeholder="搜索知识点" clearable @keyup.enter.native="handleQuery" />
        </el-form-item>
        
        <el-form-item>
          <el-button type="primary" icon="el-icon-search" @click="handleQuery">搜索</el-button>
          <el-button icon="el-icon-refresh" @click="resetQuery">重置</el-button>
        </el-form-item>
      </el-form>
      
      <!-- 排序 -->
      <div class="sort-bar">
        <el-radio-group v-model="queryParams.orderBy" @change="handleQuery">
          <el-radio-button label="hot">🔥 热门</el-radio-button>
          <el-radio-button label="latest">📅 最新</el-radio-button>
          <el-radio-button label="recommend">⭐ 推荐</el-radio-button>
        </el-radio-group>
      </div>
    </el-card>

    <!-- 知识点列表 -->
    <div class="point-list">
      <el-row :gutter="20">
        <el-col :xs="24" :sm="12" :md="8" :lg="6" v-for="point in pointList" :key="point.pointId">
          <point-card :point="point" @click="handlePointClick(point.pointId)" />
        </el-col>
      </el-row>
    </div>

    <!-- 分页 -->
    <pagination
      v-show="total > 0"
      :total="total"
      :page.sync="queryParams.pageNum"
      :limit.sync="queryParams.pageSize"
      @pagination="getList"
    />
  </div>
</template>

<script>
import { getSubjectList, getChapterTree, getPointList } from '@/api/knowledge'
import PointCard from '@/components/knowledge/PointCard'

export default {
  name: 'KnowledgeList',
  components: { PointCard },
  data() {
    return {
      // 查询参数
      queryParams: {
        pageNum: 1,
        pageSize: 12,
        subjectId: undefined,
        chapterId: undefined,
        difficulty: undefined,
        keyword: undefined,
        orderBy: 'hot'
      },
      // 科目列表
      subjectList: [],
      // 章节列表
      chapterList: [],
      // 知识点列表
      pointList: [],
      // 总条数
      total: 0,
      // 加载状态
      loading: false
    }
  },
  created() {
    this.getSubjectList()
    this.getList()
  },
  methods: {
    /** 查询科目列表 */
    getSubjectList() {
      getSubjectList().then(response => {
        this.subjectList = response.data
      })
    },
    /** 科目变化时查询章节 */
    handleSubjectChange(subjectId) {
      this.queryParams.chapterId = undefined
      this.chapterList = []
      if (subjectId) {
        getChapterTree(subjectId).then(response => {
          this.chapterList = response.data
        })
      }
      this.handleQuery()
    },
    /** 查询知识点列表 */
    getList() {
      this.loading = true
      getPointList(this.queryParams).then(response => {
        this.pointList = response.rows
        this.total = response.total
        this.loading = false
      })
    },
    /** 搜索按钮操作 */
    handleQuery() {
      this.queryParams.pageNum = 1
      this.getList()
    },
    /** 重置按钮操作 */
    resetQuery() {
      this.queryParams = {
        pageNum: 1,
        pageSize: 12,
        subjectId: undefined,
        chapterId: undefined,
        difficulty: undefined,
        keyword: undefined,
        orderBy: 'hot'
      }
      this.getList()
    },
    /** 点击知识点卡片 */
    handlePointClick(pointId) {
      this.$router.push({ path: `/knowledge/detail/${pointId}` })
    }
  }
}
</script>

<style scoped lang="scss">
.knowledge-list-container {
  padding: 20px;
}

.filter-card {
  margin-bottom: 20px;
}

.sort-bar {
  margin-top: 15px;
}

.point-list {
  margin-top: 20px;
  min-height: 400px;
}
</style>
```

### 3.3 知识点卡片组件

#### 文件：src/components/knowledge/PointCard.vue

```vue
<template>
  <el-card class="point-card" shadow="hover" @click.native="handleClick">
    <!-- 标签 -->
    <div class="tags">
      <el-tag
        v-for="tag in point.tags"
        :key="tag.tagId"
        :color="tag.tagColor"
        size="mini"
        effect="dark"
      >
        {{ tag.tagName }}
      </el-tag>
    </div>

    <!-- 标题 -->
    <h3 class="title">{{ point.title }}</h3>

    <!-- 摘要 -->
    <p class="summary">{{ point.summary }}</p>

    <!-- 信息栏 -->
    <div class="info-bar">
      <span class="info-item">
        <i class="el-icon-view"></i>
        {{ point.viewCount }}
      </span>
      <span class="info-item">
        <i class="el-icon-star-on"></i>
        {{ point.likeCount }}
      </span>
      <span class="info-item">
        <i class="el-icon-collection"></i>
        {{ point.collectCount }}
      </span>
    </div>

    <!-- 底部信息 -->
    <div class="footer">
      <span class="author">{{ point.authorName }}</span>
      <span class="difficulty">
        <el-tag :type="getDifficultyType(point.difficulty)" size="mini">
          {{ getDifficultyText(point.difficulty) }}
        </el-tag>
      </span>
    </div>
  </el-card>
</template>

<script>
export default {
  name: 'PointCard',
  props: {
    point: {
      type: Object,
      required: true
    }
  },
  methods: {
    handleClick() {
      this.$emit('click')
    },
    getDifficultyType(difficulty) {
      const types = { 1: 'success', 2: 'warning', 3: 'danger' }
      return types[difficulty] || ''
    },
    getDifficultyText(difficulty) {
      const texts = { 1: '简单', 2: '中等', 3: '困难' }
      return texts[difficulty] || ''
    }
  }
}
</script>

<style scoped lang="scss">
.point-card {
  margin-bottom: 20px;
  cursor: pointer;
  transition: transform 0.2s;

  &:hover {
    transform: translateY(-5px);
  }

  .tags {
    margin-bottom: 10px;
    min-height: 20px;
  }

  .title {
    font-size: 16px;
    font-weight: bold;
    margin: 10px 0;
    overflow: hidden;
    text-overflow: ellipsis;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
  }

  .summary {
    font-size: 14px;
    color: #666;
    line-height: 1.5;
    overflow: hidden;
    text-overflow: ellipsis;
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    min-height: 63px;
  }

  .info-bar {
    display: flex;
    justify-content: space-around;
    margin: 15px 0;
    padding-top: 15px;
    border-top: 1px solid #eee;

    .info-item {
      font-size: 14px;
      color: #999;

      i {
        margin-right: 5px;
      }
    }
  }

  .footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 12px;
    color: #999;
  }
}
</style>
```

---

## 四、开发规范

### 4.1 代码规范

**Java规范**:
- 遵循阿里巴巴Java开发手册
- 类名使用大驼峰命名（PascalCase）
- 方法名、变量名使用小驼峰命名（camelCase）
- 常量使用全大写加下划线（SNAKE_CASE）
- 每个方法必须添加注释说明

**JavaScript规范**:
- 使用ES6+语法
- 使用2空格缩进
- 变量名、函数名使用小驼峰命名
- 组件名使用大驼峰命名
- 文件名使用短横线命名（kebab-case）

### 4.2 数据库规范

- 表名使用小写加下划线
- 字段名使用小写加下划线
- 必须添加字段注释
- 合理设置索引
- 时间字段统一使用DATETIME类型

### 4.3 接口规范

**RESTful API规范**:
- GET：查询操作
- POST：新增操作
- PUT：修改操作
- DELETE：删除操作

**返回格式**:
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {}
}
```

### 4.4 Git提交规范

```bash
feat: 新增知识点列表页面
fix: 修复知识点详情页加载问题
docs: 更新开发文档
style: 优化知识点卡片样式
refactor: 重构知识点Service层
test: 添加知识点单元测试
```

---

## 五、测试方案

### 5.1 单元测试

**后端单元测试**:
```java
@RunWith(SpringRunner.class)
@SpringBootTest
public class KnowledgePointServiceTest {
    
    @Autowired
    private IKnowledgePointService knowledgePointService;
    
    @Test
    public void testSelectKnowledgePointByPointId() {
        KnowledgePointVO point = knowledgePointService.selectKnowledgePointByPointId(1L, 1L);
        assertNotNull(point);
        assertEquals("Cache映射方式详解", point.getTitle());
    }
    
    @Test
    public void testInsertKnowledgePoint() {
        KnowledgePoint point = new KnowledgePoint();
        point.setSubjectId(1L);
        point.setChapterId(11L);
        point.setTitle("测试知识点");
        point.setContent("# 测试内容");
        point.setAuthorId(1L);
        point.setAuthorName("admin");
        
        int result = knowledgePointService.insertKnowledgePoint(point);
        assertEquals(1, result);
    }
}
```

### 5.2 接口测试

使用Postman或Apifox进行接口测试：

1. **获取知识点列表**
   ```
   GET http://localhost:8080/student/knowledge/points?pageNum=1&pageSize=10
   ```

2. **获取知识点详情**
   ```
   GET http://localhost:8080/student/knowledge/point/1
   ```

3. **创建知识点**
   ```
   POST http://localhost:8080/student/knowledge/point/create
   Content-Type: application/json
   
   {
     "subjectId": 1,
     "chapterId": 11,
     "title": "测试知识点",
     "content": "# 测试内容"
   }
   ```

### 5.3 功能测试

测试用例清单：

| 功能模块 | 测试用例 | 预期结果 |
|---------|---------|---------|
| 知识点列表 | 分页查询 | 正确显示知识点列表 |
| 知识点详情 | 查看详情 | 正确显示内容和互动数据 |
| 知识点创建 | 学生创建知识点 | 提交审核，状态为"审核中" |
| 知识点审核 | 管理员审核 | 审核通过后状态变为"已发布" |
| 点赞功能 | 点赞/取消点赞 | 点赞数正确变化 |
| 收藏功能 | 收藏/取消收藏 | 收藏数正确变化 |
| 评论功能 | 发表评论 | 评论成功，评论数+1 |
| 学习记录 | 学习时长统计 | 正确记录学习时长 |

---

## 六、总结

本文档详细介绍了知识点库系统的开发实施方案，包括：

1. ✅ 开发环境准备
2. ✅ 后端开发指南（实体类、Mapper、Service、Controller）
3. ✅ 前端开发指南（API封装、页面组件）
4. ✅ 开发规范（代码、数据库、接口、Git）
5. ✅ 测试方案（单元测试、接口测试、功能测试）

**开发建议**:
- 先完成后端接口开发和测试
- 再进行前端页面开发
- 采用前后端分离的开发模式
- 及时进行代码Review和单元测试

**注意事项**:
- Markdown内容需要同时保存原文和HTML
- 点赞、收藏、评论等互动数据需要实时更新
- 知识点审核流程需要严格把控内容质量
- 学习记录需要防止刷数据

---

> **下一步行动**:  
> 1. ✅ 已创建数据库表结构SQL  
> 2. ⏳ 开始后端开发（实体类、Mapper、Service、Controller）  
> 3. ⏳ 开始前端开发（API、页面、组件）  
> 4. ⏳ 测试与优化
