# 1）sql

```sql
CREATE TABLE daming_follows (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键ID',
    follower_id BIGINT NOT NULL COMMENT '关注者ID (粉丝)',
    following_id BIGINT NOT NULL COMMENT '被关注者ID (偶像)',
    create_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '关注时间',
    
    -- 建立唯一索引，防止重复关注
    UNIQUE KEY unique_follow (follower_id, following_id),
    -- 为被关注者建立索引，方便查询“我的粉丝”
    INDEX idx_following (following_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户关注关系表';
```

# 2）思路

1.接口路径参数接收用户id和关注true还是取关

2.查询文章的时候再查下该文章下的所有粉丝有无观看者，有的话显示对应信息

>wrapper eq，userId和followUserId查出来count（）大于0则是已关注，返回已关注，前端对应渲染

# 3）共同关注业务

